import { CommandInteraction, Guild, GuildMember, Message, TextBasedChannel } from "discord.js";
interface opt {
    content: string;
    ephemeral: boolean;
}
export default class RootComponent {
    entity: Message | CommandInteraction;
    type: "message" | "interaction";
    channel: TextBasedChannel;
    guild: Guild;
    hostMember: GuildMember;
    reply: {
        message: (options: opt) => Promise<void> | Promise<Message<boolean>>;
        interaction: (options: opt) => Promise<void> | Promise<Message<boolean>>;
    };
    followUp: any;
    constructor(root: Message | CommandInteraction);
}
export {};

import { SlashCommandBuilder } from "@discordjs/builders";
import { CommandInteraction } from "discord.js";
import { TriviaCommandBuilderOptions, TriviaGameOptions } from "../Typings/interfaces";
export default class TriviaCommandBuilder {
    private build;
    gameOptions: TriviaGameOptions;
    private isApplied;
    constructor(options?: TriviaCommandBuilderOptions);
    private optionApplicators;
    private applyOptions;
    toJSON(): import("discord-api-types").RESTPostAPIApplicationCommandsJSONBody;
    toBuilder(): SlashCommandBuilder;
    getOptions(int: CommandInteraction, additionalOptions?: Partial<TriviaGameOptions>): TriviaGameOptions;
}

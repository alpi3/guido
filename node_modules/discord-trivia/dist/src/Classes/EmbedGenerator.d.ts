import { MessageEmbed } from "discord.js";
import { Question } from "open-trivia-db";
import TriviaGame from "./TriviaGame";
export default class EmbedGenerator {
    private readonly game;
    private readonly theme;
    constructor(game: TriviaGame);
    gameQueueStart(): MessageEmbed;
    gameStart(): MessageEmbed;
    leaderboardUpdate(question: Question): MessageEmbed;
    finalLeaderboard(): MessageEmbed;
    question(question: Question): MessageEmbed;
}
